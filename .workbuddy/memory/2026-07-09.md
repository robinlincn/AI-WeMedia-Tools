# 2026-07-09 工作日志

## 产品规划初始化
- 将老板的 AI-WeMedia-Tools 长期规划写成根目录 `README.md`：项目愿景、7 个分目录结构、三阶段演进路线图（Skills→独立小软件→整合大型工具箱）、模块进度表、版本管理说明。
- 同步写入长期记忆 `.workbuddy/memory/MEMORY.md`，与 README 保持一致。
- 配置 `.gitignore`：排除 `node_modules/`、`videos/`、`frames/`、`OutVideos/`、`logs/` 及系统垃圾；仅保留 `.workbuddy/memory/`。
  - 关键原因：`AI-Prompts-Tools/Prompts-Back-Calculate` 内含 node_modules 约 7.3GB，必须排除，否则无法推 GitHub。
- 初始化 git 仓库，配置 user（robinlin / robinlin@chinamustsee），添加 SSH 远程，首次提交并推送至 GitHub（main 分支）。

## 备注
- GitHub 推送使用 SSH 认证，已验证账号为 robinlincn。
- 后续阶段推进时，更新 README 进度表并重新提交推送。

## AI-Articles-Tools 模块落地（采集 + 二创）
- 在 `AI-Articles-Tools/Codes/` 落地完整代码：采集（text/link/video 三种）+ 二次创作（标题/正文/配图/头条排版），SQLite 持久化（sources/outputs/media 三表）。
- 关键文件：`src/cli.py`（collect/pipeline/create/list）、`src/creator/rewrite.py`（编排）、`src/creator/llm.py`（OpenAI 兼容 + mock）、`src/db.py`、`src/collector/*`（三种采集器）。
- mock 模式可离线跑通全流程（确定性返回，无需 API key）。
- 新增 `AI-Articles-Tools/Codes/README.md`（模块使用文档）与 `tests/test_pipeline.py`（离线冒烟测试 + 回归断言）。
- 新增 `.gitignore` 规则：`*.db`、`AI-Articles-Tools/Articles/`、`AI-Articles-Tools/OutArticles/`（生成产物本地保留、不入库）。
- README 进度表与 MEMORY.md 同步：AI-Articles-Tools 标记为 🟢 进行中。

### 修复的 bug（重要）
- 现象：二创正文整段被替换成标题文本「【干货】…」，相似度虽标记 0.05 但内容错误。
- 根因：`src/creator/llm.py` 的 `_mock_reply` 用 `"标题" in system` 判定是否标题任务；而 `REWRITE_SYSTEM` 第 6 条含「小标题」三字，子串「标题」命中，导致改写任务被误判为标题任务，返回了标题模板。
- 修复：判定标记从 `"标题"` 改为改写任务独有的「标题党」（仅 `TITLE_SYSTEM` 含此词）。
- 加固：测试新增回归断言（正文须出现改写内容、标题不得作为正文段落）。

### 待办
- 考虑把「初始化一个 AI 分类模块（SQLite + mock 离线 pipeline）」沉淀为可复用 Skill（本次超过 15 步，符合积累条件）。
- 真实 LLM / 图片 / ASR 接入需配置 config.toml 或环境变量（尚未实跑真实 key）。
- 未 git 提交（待本次工作完成后 commit + push main）。

## AI-Articles-Tools 迭代（按老板反馈逐步调整）
老板给 3 个真实头条链接实测采集+二创，提出 3 点调整并逐步落地：

### 1) 采集内容补全 + 格式对齐
- 现象：原采集内容不全、格式与链接页面不一致（标题层级丢失、内联图未内联、落底噪点）。
- 根因：头条桌面版为 JS 渲染壳，`requests` 抓不到正文；原采集器只取 `<p>` 文本、把图堆到最前、丢弃 h1-h6。
- 修复（`src/collector/link.py`）：
  - 桌面版 URL 自动改写移动版服务端渲染地址 + 移动 UA（此前已实现，本轮保留）。
  - 按文档顺序遍历，保留 `h1-h6` 标题层级 / 段落 / 列表 / 引用 / 内联图；内联图取懒加载 `data-src` 真地址、下载后按出现顺序内联。
  - 过滤页脚/导航噪点（app logo、头像、loading 图、`打开App`/`相关推荐` 等 UI 文案）。
  - 链接采集文件夹前缀改为 **文章链接采集+标题+时间戳**。

### 2) 二创双风格输出
- 二创文件夹前缀改为 **文章二创+标题+时间戳**。
- 文件夹内含两份 md，可直接复制文字+图片进对应编辑器发布：
  - `头条风格+标题+时间戳.md`（导语 + 加粗首句 + 分隔线 + 互动引导「点赞/评论/关注」）
  - `公众号风格+标题+时间戳.md`（更柔和留白、无分隔线、互动引导「点赞/在看/分享/关注」）
- 图片（AI 生成配图）带中英文图注、内联于两份 md；`layout.py` 新增 `compose_wechat`，`models.py` 的 `CreationResult` 增加 `wechat_md_path`。

### 3) 顺手修复的质量问题：导语与正文首句重复
- 原 `intro = paras[0][:60]`，正文又把整段首句加粗放出 → 导语与正文首句完全重复。
- 修复（`rewrite.py` 新增 `_split_intro`）：导语只取首句作钩子，正文从首句之后接续，不再重复。
- 测试新增回归断言覆盖双风格 md、文件夹/文件名前缀。

### 实测结论（3 个真实头条链接）
- 采集：3 篇均完整抓取（标题层级 + 内联图 + 正文），落 `Articles/文章链接采集*`。
- 二创：source 9/10/11 用真实 DeepSeek 跑通双风格，标题有钩子、正文口语化无 AI 腔、配图带中英文图注、排版到位。
- 已知边界：4-gram Jaccard 相似度对「保留事实/专名」的改写过度惩罚（实测 2%~30%），真实「与原创相似度<10%」应接嵌入/查重 API 兜底——已写入代码注释与文档，待后续迭代。

### 数据库位置
- `ai_articles.db` 已迁入 `AI-Articles-Tools/DataBase/`（config `[paths] db = "DataBase/ai_articles.db"`）。

### 待办（下一轮可能要调）
- 相似度指标升级（嵌入查重或软阈值）。
- 配图中文图注质量（真实 LLM 偶发「第1行：」之类冗余前缀，可加清洗）。
- git 提交 + 推送（本轮改动未提交）。

## AI-Articles-Tools 迭代（命名规范 + 全局化）
老板提出 3 点调整，本轮落地：

### 1) 文件夹/文件名命名分隔符 `+` → `-`
- `src/collector/base.py` 的 `make_folder`：`{prefix}{title}_{ts}` → `{prefix}-{title}-{ts}`（三种采集器统一）。
- `src/creator/rewrite.py`：二创文件夹 `文章二创-{title}-{ts}`；两份 md `头条风格-{title}-{ts}.md`、`公众号风格-{title}-{ts}.md`。
- 测试新增回归断言（`文章二创-` / `头条风格-` / `公众号风格-` 前缀 + 连字符计数）。
- 老板消息里「标题」两侧的引号视为占位符，沿用不加字面引号。

### 2) 配置与数据库全局化（集中管理）
- 新增 `AI-WeMedia-Tools/AI-Global/`：
  - `AI-Global/Configs/`：`config.toml`（含密钥，gitignore）+ `config.example.toml`（模板，已 git mv 至此）。
  - `AI-Global/Database/`：`ai_articles.db`（由 `AI-Articles-Tools/DataBase/` 迁入，各分类共享）。
- `src/config.py`：
  - `load_config(None)` 默认读 `AI-Global/Configs/config.toml`，缺失退回 `config.example.toml`。
  - `resolve_paths()` 中 `db_path` 解析锚点改为**项目根目录**（`base_dir.parent`）；`articles_dir`/`out_dir` 仍锚定**分类目录**（`base_dir`）。
  - `config [paths] db = "AI-Global/Database/ai_articles.db"`。
- `src/cli.py`：`--mock` 时强制 `cfg.provider="mock"`，无配置也能离线跑通。
- 各分类的 `Articles/OutArticles` 仍留在本分类目录；仅 config 与 db 共享。
- 旧 `AI-Articles-Tools/DataBase/` 已删除（保留 `.gitkeep` 于 AI-Global）。

### 验证
- 离线测试 `tests/test_pipeline.py` 全绿（含 `-` 分隔断言）。
- 真实链接采集（不传 `--config`）自动发现全局配置、写入 `AI-Global/Database/ai_articles.db`，生成 `文章链接采集-开源 TTS…-20260709_174219`。
- 清理了上一轮 `_` 分隔的旧命名残留文件夹。
- 已 git 提交 + 推送 main。

### 待办（架构提醒）
- 当前 `ai_articles.db` 表结构（sources/outputs/media）偏文章语义；若后续多分类共用同一库，需按分类加 `category` 列或表命名空间，避免数据混淆（本轮先按老板要求做位置集中化）。

## 沉淀 Skills 技能包（AI-WeMedia-Skills）
- 老板要求把今天的全部操作形成 Skills 技能包，存到 `AI-WeMedia-Tools/AI-WeMedia-Skills/`。
- 创建 `AI-WeMedia-Skills/ai-category-module-scaffold/SKILL.md`（综合版技能）：覆盖目录约定、命名规范（`-` 分隔）、全局 config/db 模式（AI-Global）、三类采集器+双风格二创、`db.py` 三表、`rewrite.py` 编排要点，以及**三大已知坑与修复**（mock 改写误判标题 / 头条移动版 SSR / 导语重复）。
- 创建 `AI-WeMedia-Skills/README.md` 作为技能包索引。
- 删除旧的用户级过时副本 `~/.workbuddy/skills/ai-category-module-scaffold/`（避免同名盖掉新版），权威版现位于项目内 `AI-WeMedia-Skills/`。
- 待 git 提交 + 推送 main（含本日志与技能包）。


## 分类技能包改为「就近分开放置」（老板新要求）
- 老板要求：把每个分类形成一个单独的 Skills 技能包，**分开放置在各自分类文件夹内**（如 `AI-Articles-Tools/AI-Articles-Skills/`），不再集中放在 `AI-WeMedia-Skills/`。
- 已落地：
  - `AI-Articles-Tools/AI-Articles-Skills/SKILL.md` —— **完整可运行的使用/操作型技能**（采集-二创-mock 全流程-命令参考-双风格排版-三大已知坑排错）。
  - 其余 6 个分类各建 `AI-*-Tools/AI-*-Skills/SKILL.md` 占位技能（Images/Musics/Sounds/Videos/Webs 为 🟡 规划中，引用脚手架落地；Prompts 注明已有 Prompts-Back-Calculate 初步实现、🟢 进行中）。
- 决策：`AI-WeMedia-Skills/ai-category-module-scaffold/` 作为**跨分类的「模块脚手架」元技能保留**（不属于任何单一分类，用于新建分类），各分类占位技能均引用它落地。
- 同步更新：根 README「🧩 分类技能包（Skills）布局」小节；`AI-WeMedia-Skills/README.md` 重写为全局脚手架索引 + 各分类技能位置表；`MEMORY.md` 新增「技能包布局约定」。
- 待 git 提交 + 推送 main（本次新增 7 个分类技能包 + 文档更新）。
