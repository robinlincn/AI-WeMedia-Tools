# 2026-08-05 工作日志

## AI-Images-Skills：真实 ComfyUI 连通与 Boogu 工作流侦察

### 环境变化
- 局域网 ComfyUI 现已可达：`http://192.168.31.243:8188/`（之前超时）。
- 设备：NVIDIA GeForce RTX 5070 Ti，cuda，显存 16GB（空闲约 14.7GB）。

### 关键侦察结论（已验证）
- **`image_boogu_image_0_1_turbo_t2i` 不是节点类名，也不是任何节点的下拉选项——它是用户机器上“保存的一个工作流文件（.json）”**。
- ComfyUI 标准 `/prompt` API 只能用完整 graph 调用，**不能按名字加载工作流**。
- 已探测确认服务端无工作流读取/枚举接口：`/api/workflows`、`/workflows`、`/boogu`、`/api/userdata`、`/api/bookmarks/workflows` 等均 404/400；仅 `/api/extensions` 200。`/system_stats`、`/object_info`、`/prompt`、`/history`、`/queue` 正常。

### Boogu 相关资产（已确认存在于服务器）
- `UNETLoader` 选项含 `boogu_image_turbo_fp8_scaled.safetensors`、`boogu_image_edit_fp8_scaled.safetensors`。
- 节点 `TextEncodeBooguEdit`：输入 `clip`(CLIP)、`vae`(VAE)、`prompt`(STRING)、`negative_prompt`(STRING)、`images`(COMFY_AUTOGROW_V3，可选)；输出 `[CONDITIONING, CONDITIONING]`（正/负）。
- `CLIPLoader.type` 含专属值 `'boogu'`（同列表还有 krea2/joyimage/qwen_image/hunyuan_image 等模型专属类型）——说明 Boogu 用专属 CLIP 加载方式，但具体 CLIP 文件名、VAE 文件名、采样参数都写在用户保存的工作流 JSON 里，object_info 静态全集无法按 type 过滤出对应文件。

### 已验证
- 用技能包自带的 `ComfyClient`（`src/comfy_client.py`，纯标准库 urllib）跑真实 `check_connectivity()` → OK；`/queue` 可访问且空闲。证明“我的客户端 → 真实服务器”链路本身没问题。

### 当前阻塞 & 下一步
- **阻塞点**：要精确运行用户指定的工作流，必须拿到它的 **API 格式 JSON**（ComfyUI 菜单 → Export (API Format)）。
- 拿到后运行命令（run.py 已就绪）：
  `python -m src.run --workflow image_boogu_image_0_1_turbo_t2i --set positive_prompt="..." --set seed=42`
- 已告知用户：导出该 JSON 放入 `AI-Images-Tools/AI-Images-Skills/workflows/`，或直接粘贴内容，即可一键生成测试图。

### ✅ 真机出图成功（用户贴出工作流 JSON 后）
- 用户直接粘贴了 `image_boogu_image_0_1_turbo_t2i` 的 API 格式 JSON（完整图，节点 33/35/34:*）。
- 关键实现细节（已确认）：`CLIPLoader(type='boogu', clip_name='qwen3vl_8b_fp8_scaled.safetensors')` + 标准 `CLIPTextEncode`（非 TextEncodeBooguEdit）；负向用 `ConditioningZeroOut` 置零；`ResolutionSelector(1:1, 1MP, multiple 8)` 出宽高；`KSampler(steps=4, cfg=1, sampler=lcm, scheduler=sgm_uniform)`。
- 已落盘为技能包正式模板：`AI-Images-Tools/AI-Images-Skills/workflows/image_boogu_image_0_1_turbo_t2i.json` + 同名 `.params.json`（映射 positive_prompt→34:11.text、seed→34:32.seed、aspect_ratio→35.aspect_ratio）。
- 运行命令：`python src/run.py --workflow image_boogu_image_0_1_turbo_t2i`（--set 可覆盖参数）。
- 结果：prompt_id=577e079c-efb9-4587-b55e-21b720d048ee；产物 `OutImages/577e079c-.../33_Boogu_00010_.png`；**PNG 校验通过 1024×1024、2240KB**；耗时 145s（首跑含模型加载，显存驻留后更快）。
- 结论：AI-Images-Skills 客户端 → 局域网 ComfyUI 的「提交/轮询/下载落盘」真实链路完全可用。

### 待办
- AI-Images-Skills 技能包（含本次新增的 boogu 文生图模板）**尚未 git commit + push**（上轮被用户中断）。待图片任务确认后一并提交。
